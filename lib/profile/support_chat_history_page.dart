import 'package:flutter/material.dart';

// Заглушка: история чатов отключена для пользовательской части.
class SupportChatHistoryPage extends StatelessWidget {
  const SupportChatHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SizedBox.shrink());
  }
}

